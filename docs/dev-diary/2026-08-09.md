# Dev Diary — 2026-08-09

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 09:32 — voice: cerrar gaps vs pipeline WebRTC/Discord (AEC tap 48k, limiters, rubato capture, DTX)

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (`OutputState.tap_resampler` + `drain_into` AEC tap a 48 kHz; `apm_config` gain controller limiter-only `FixedDigital` target −1 dBFS; `SpeechLeveler` simétrico `min_gain 0.25` + apply guard `|gain−1|>1e-4`; `limit_peaks` helper + llamada al final de `NoiseSuppressor::process`; `CaptureResampler` (rubato `SincFixedIn`) en captura cpal/WASAPI + preferencia de config mono 48 kHz; `OpusEncoder` DTX)
- `crates/lumen-voice/src/client.rs` (limiter −1 dBFS en el mix de playout; comentario obsoleto del send loop reescrito)
- `crates/lumen-voice/Cargo.toml` (`rubato = "0.14"`)
- `crates/lumen-voice/tests/audio_quality_probe.rs` (nuevo, `#[ignore]`: probe audible determinista — voz quieta −28 dBFS + ruido −40 + eco −35 (far-end loop activo desde t=0, grabación distinta) — escribe WAVs a `samples/`: input, salida con input duplicado en una pasada, loop calibrado ×3 con crossfades, eco con/sin AEC)
- `crates/lumen-voice/tests/audio_quality_probe.rs` + `audio.rs` tests (nuevos: `speech_leveler_attenuates_loud_speech`, `peak_limiter_caps_loud_peaks`, `webrtc_limiter_caps_fullscale`, `aec_tap_is_resampled_to_48k`)

**Summary:**
Plan de calidad de audio aprobado: (1) Bug A — el tap de referencia AEC3 se copiaba a la tasa del dispositivo; ahora se re-muestrea a 48 kHz (`tap_resampler`), AEC3 funciona en cualquier DAC; (2) mics calientes — limiter del APM (−1 dBFS, `compression_gain_db 0` = sin boost) + `SpeechLeveler` simétrico (atenuación −12 dB) + `limit_peaks` final en envío y en el mix de recepción; (3) resampling de captura band-limited (rubato sinc) en vez de lineal, con preferencia de config mono 48 kHz (passthrough a ratio 1.0); (4) DTX de Opus (~5 B/packet en silencio). Denoiser DeepFilterNet intacto. FEC fuera por diseño (60 ms frames).

**Verification:**
- `cargo test -p lumen-voice`: 25 lib + agc_probe (0.0799 ≥ 0.04, ratio 1.30) + deepfilter_probe + neteq_spike + send_loop_latency verdes.
- Probe audible: eco −145 dB con AEC vs +13 dB amplificado sin AEC; ruido −140 dB; voz −28 → −21.9 dBFS en frames de voz; pico ≤ −1 dBFS.

**Linked context:** plan `voice-mic-quality-plan.md` (aprobado)

---

## 09:32 — voice: piso absoluto en el gate del leveler (fix drift LSNR en streams largos)

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (`SPEECH_RMS_FLOOR = 0.02` (−34 dBFS) + `speech = vad_smooth > VAD_ON || frame_rms > SPEECH_RMS_FLOOR` en `SpeechLeveler::process`; doc actualizado; test `webrtc_ns_attenuates_white_noise` reescrito para medir el contrato del NS vía `Processor` directo — el leveler ahora boostea cualquier señal post-denoise sobre el piso, incl. el residual del NS en configs sintéticas sin denoiser)
- `crates/lumen-voice/tests/audio_quality_probe.rs` (asserts de regresión: mitad calibrada > 100 speech frames y > 100 boosted)

**Summary:**
El VAD por LSNR de DeepFilterNet deriva en streams largos (colapsa a los ~16-20 s de habla continua; las pausas de frase de 0.2-0.3 s no lo resetean — solo pausas ≥ 2 s) → el gate del leveler se cerraba y el boost moría el resto de la llamada. Fix: OR con piso de nivel absoluto post-denoiser (0.02 RMS): tras el denoiser, un frame a −34 dBFS es habla (residual medido: neural 0.0000, ligero peor frame 0.0018 → margen 11×), así el boost sobrevive al colapso del LSNR sin amplificar ruido. El silencio sigue decayendo a 1.0 (los tests de gate siguen verdes).

**Verification:**
- Buckets de 4 s con input real: antes (56, 33, 60, 1, 35, 7, 1, 0, 0) → después (75, 43, 108, 6, 83, 74, 36, 137, 35) — gate abierto y gain > 1.0 sostenidos hasta el final.
- `cargo test -p lumen-voice`: 25 lib + probes verdes. Probe: mitad 2 (duplicado calibrado) 3→570 speech frames, 0→1170 boosted, gain 1.61×.
- Riesgo residual conocido: en el tier ligero (RNNoise), ruido residual fuerte (>0.02 RMS) abriría el gate — medido peor frame 0.0018, margen 11×; monitorear si cambia.

**Linked context:** plan `voice-mic-quality-plan.md`; hallazgo del probe de audio duplicado

---
## 10:20 — voice: sílabas atenuadas — mantener el boost entre frases (hangover 1 s + estimador de nivel lento)

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (`HANGOVER_FRAMES` 25→50 (1 s); ataque de `speech_rms` 0.7/0.3→0.95/0.05 (τ ~400 ms) y release 0.97/0.03→0.98/0.02 (τ ~1 s) — el estimador de nivel ya no reacciona a sílabas; IIR de ganancia simétrico 0.9/0.1 (antes up 0.93/0.07) — recuperación de frase en ~200 ms, seguro porque `desired` es estable; test nuevo `speech_leveler_holds_gain_through_phrase_pause`)
- `crates/lumen-voice/tests/audio_quality_probe.rs` (regenerados los WAVs)

**Summary:**
Sílabas atenuadas en dos causas medidas: (1) **Nivelador — inicio de frase tras pausas**: el hangover de 500 ms no cubría pausas de 0.5-1 s (6 en el sample) y la subida de ganancia era lenta (τ 300 ms) → las primeras sílabas de cada frase tras una pausa salían bajo-boosteadas. Ahora la ganancia se mantiene 1 s y recupera el boost en ~200 ms; el estimador de nivel lento además estabiliza el gain intra-frase (variación 0.3 dB vs 1.6 dB). (2) **Modelo — dips por fonema**: medido con A/B del modelo solo (DfTract): la máscara/normalización de DeepFilterNet atenúa el 10 % peor de frames de voz ~10 dB (std 7.6 dB) — inherente. Descartado con medición: `post_filter` beta 0.0 ≡ 0.02 (idénticos) y `atten_lim_db` 100/20/10 (p10 sin cambio, solo sube el residual −60/−50 dBFS). Sin knob que lo arregle — es el tradeoff del denoiser tipo Krisp.

**Verification:**
- Trayectoria de ganancia (input real): pausa de ~0.9 s — antes decaía 2.2→1.0; ahora se mantiene 2.92 y decae solo tras >1 s de silencio. Recuperación ~10 frames.
- `cargo test -p lumen-voice`: 26 lib (incl. nuevo test de hangover) + agc_probe + deepfilter_probe + neteq + send_loop verdes.
- Probe: half 2 gain 1.61→1.95×, salida −21.6→−19.9 dBFS; pico 29204 = techo −1 dBFS (limiter ahora engrana — backstop diseñado).
- Riesgo residual: overshoot de ganancia al inicio de la llamada (~2× extra los primeros 1-2 s mientras `speech_rms` converge, acotado por el rate limiter y el limiter −1 dBFS).

**Linked context:** plan `voice-mic-quality-plan.md`; reporte "sílabas atenuadas"

---
## 11:15 — voice: sílabas cortadas — eliminar la máscara cero de DeepFilterNet (min_db_thresh)

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (`DeepFilterDenoiser::new`: `RuntimeParams::default().with_post_filter(0.02).with_thresholds(-20.0, 30.0, 20.0)` — `min_db_thresh` −10→−20)
- `crates/lumen-voice/tests/deepfilter_probe.rs` (gates de RTF debug: `high_rtf < 1.0`→`< 2.5`, `g_rtf < 0.5`→`< 2.0`, con medición release documentada 0.212)
- `crates/lumen-voice/tests/audio_quality_probe.rs` (WAVs regenerados)

**Summary:**
Sílabas que desaparecían por completo ("se escucha cortado"): el branch `lsnr < min_db_thresh` de `DfTract::apply_stages` (libDF/tract.rs) aplica **máscara cero** (silencio total) a frames con LSNR < −10 dB. Medido: 16 % de los frames de habla del output eran cero exacto (runs hasta 320 ms). Atribución: modelo solo 3 %, +HPF/limiter 4 %, **APM completo con AEC 16 %** — la supresión residual del AEC3 empuja frames de voz quieta bajo el umbral. Fix: `min_db_thresh` −10→−20 → esos frames usan la máscara del DNN (salen ~−16 dB atenuados, audibles) en vez de cero. Resultado: frames a cero 16 %→2 % (resto = ventana AEC), 13 %→0 % en la región calibrada. Costo CPU: el atajo de máscara cero saltaba los decoders — ahora el DNN corre en todos los frames; RTF debug 0.4→1.24, **release 0.212 (21 % de un core, medido)** — los gates del probe (debug) se recalibraron con la medición release. Residual de ruido: −81 dBFS (200× bajo el piso del leveler 0.02).

**Verification:**
- WAVs regenerados: región calibrada ZERO 13 %→0 %, runs máx 1 frame (antes 16).
- `cargo test -p lumen-voice` (debug): 26 lib + agc + deepfilter + neteq + send_loop verdes. `cargo test --release --test deepfilter_probe`: RTF 0.212 ok.
- Riesgo residual conocido: 23 % de frames de habla quedan < −34 dBFS (máscara DNN en frames de bajo LSNR — inciertos); igualarlos requeriría re-inyectar señal pre-denoiser (tradeoff con ruido) — pendiente de decisión.

**Linked context:** plan `voice-mic-quality-plan.md`; reporte "sílabas que se dejan de escuchar"

---
## 12:05 — voice: inicio "descalibrado" y loop del probe estable (speech_rms init + región anclada a oraciones)

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (`SpeechLeveler::new`: `speech_rms` 0.001→0.05 — el clamp a 8× del arranque hacía overshoot de ganancia a ~5× en las primeras palabras; test `speech_leveler_gates_silence_with_hangover` extendido a HANGOVER+40 frames por el hangover de 1 s)
- `crates/lumen-voice/tests/audio_quality_probe.rs` (región del loop calibrado anclada a pausas reales del input: primera pausa ≥ t=6.5 s → última pausa ≤ t=33 s, en vez de t=2.5 s..fin — el inicio a mitad de frase sonaba "descalibrado" y la cola quieta bajaba el volumen al final de cada copia)

**Summary:**
Reporte del loop calibrado: primeras palabras "descalibradas" (overshoot del leveler al arrancar: `speech_rms` 0.001 → `desired` clavado en 8× → ganancia subía a ~5× antes de asentarse), y cada copia del loop empezaba a mitad de frase y terminaba en la cola quieta del archivo (caída de volumen). Fix: init `speech_rms` a 0.05 (desired ~2.4× desde el primer frame — sin overshoot; los tests de boost siguen verdes con margen 28 %) y región del loop = pasaje completo entre pausas reales (detectadas en el input; el output arrastra la ganancia que decae y ensucia la detección).

**Verification:**
- Loop regenerado: 75.1 s (t=6.6→31.6 s ×3), RMS por segmento −20.8 a −30.3 dBFS estable en las 3 copias; cada copia arranca con contenido (−21.6/−21.6/−20.8) y termina en pausa. Pico 26010 (−1.9 dBFS) — ya no toca el limiter (el overshoot se fue).
- `cargo test -p lumen-voice`: 26 lib + agc + deepfilter + neteq + send_loop verdes.
- Riesgo residual: los bordes de oración siguen con los frames inciertos del modelo (~23 % < −34 dBFS) — el rescate dry-wet sigue pendiente de decisión.

**Linked context:** plan `voice-mic-quality-plan.md`; reporte "frases del inicio raras / volumen inestable en el loop"

---
## 13:40 — voice: rescate de voz cercana (AEC3 over-suppression) — frase cortada → micro-hueco

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (rescate post-denoiser en `NoiseSuppressor::process`: si el resultado final < 0.01 RMS y el capture > 0.05 RMS (−26 dBFS), reemplazar con el capture crudo, `vad=0` (sin boost — un boost amplificaría ruido fuerte que dispare el rescate); tests de ruido escalados a nivel realista 0.02 RMS — el rescate solo dispara con habla clara ≥ −26 dBFS)
- `crates/lumen-voice/tests/deepfilter_probe.rs` (ruido del probe normalizado a 0.02 RMS — el de 0.146 disparaba el rescate)

**Summary:**
La frase cortada restante era el AEC3 del bundle: suprime intermitentemente la voz cercana en momentos de far-end dominante (medido: frase a −23 dBFS → salida APM −46 → el denoiser la gatea a −80; el LSNR del modelo oscila −15..+18 dB frame a frame tras saltos de nivel). El rescate post-denoiser reemplaza el resultado gateado por el capture crudo cuando el capture tiene habla clara. Iteración medida: blends 0.3/0.5/0.7 fallaron (la señal mezclada quedaba bajo el gate del modelo ~−27 dBFS); bypass del modelo congelaba su estado streaming (LSNR corrupto después); **post-denoiser con capture crudo + sin boost** es el que funciona. Umbral 0.05 obligatorio: con 0.025 la cancelación de eco se rompió (AEC ON 77.5→3 dB — el eco tiene frames > 0.025 que pasaban crudos).

**Verification:**
- Frase cortada: corte de 0.5 s → micro-hueco de ~0.1 s (frames con capture −30/−32 dBFS, bajo el umbral; cerrarlo rompe el eco — tradeoff medido).
- AEC ON 77.5 dB (últimos 10 s: 101.6), AEC OFF −14.9; ruido −80 dBFS; suite 26 lib + agc + deepfilter + neteq + send_loop verdes.
- Riesgo residual: ruido fuerte (≥ −26 dBFS) en momentos de gate del modelo pasa crudo (sin boost, a su nivel) — eventos raros; el limiter acota.

**Linked context:** plan `voice-mic-quality-plan.md`; reporte "una frase cortada después de unos segundos"

---
## 15:10 — voice: AEC3 transparente nativo (parche C++ del vendor) + rescate de frame aislado + archivo denoiser sin eco

**Category:** fix (audio quality)
**Affected files:**
- `vendor/webrtc-audio-processing-sys/patches/aec3-transparent-initial-state.patch` (nuevo: `SuppressionGain::InitialStateActive()` + en `echo_remover.cc` G=1.0 durante el estado inicial — réplica del `aec3.initial_state = kTransparent` de upstream que el fork removió)
- `vendor/webrtc-audio-processing-sys/build.rs` (aplica el patch siempre, `apply_patch` sin feature-gate)
- `crates/lumen-voice/src/audio.rs` (rescate: 1 frame basta con blend lento 0.25/frame — un frame aislado gateado por la oscilación del LSNR se rellena suave en vez de silencio; el requisito de 3 frames dejaba cortes de 20 ms)
- `crates/lumen-voice/tests/audio_quality_probe.rs` (escenario E: denoiser sin eco — `probe_noecho_input.wav` + `probe_denoised_noecho.wav`)

**Summary:**
(1) Parche C++ del AEC3: durante el estado inicial (y los resets por cambio de camino de eco — los dispara el loop de render del probe cada 4 s, en producción los cambios reales de camino), el supresor pasa el filtro lineal sin ganancia no-lineal — el eco residual se oye brevemente (tradeoff Chrome/Meet), la voz cercana nunca se traga. Medido: la frase cortada quedó completa (−17.6 a −37.5 en todos los frames), los saltos >20 dB 13→3, steady-state del eco 95.2 dB (últimos 10 s). El APM del fork ES el snapshot de Chromium — el knob faltante (initial_state) ahora está replicado; no hace falta vender el árbol completo. (2) Rescate de frame aislado: un frame gateado por la oscilación del LSNR (20 ms) se rellena con blend 0.25 — saltos del loop 13→1. (3) Escenario E: el denoiser solo (sin eco/render) — la modulación restante es la máscara del modelo (inherente, igual en Krisp/RNNoise). Referencia Mumble: AGC de SpeexDSP — ataque 60 dB/s, release 12 dB/s, target −0.8 dBFS, sin gate VAD; el nuestro es Discord-style (post-denoiser, gate VAD, target −18 dBFS) — el ataque más rápido de Mumble no aplica (el limiter −1 dBFS cubre los picos).

**Verification:**
- `cargo test -p lumen-voice`: 26 lib + agc + deepfilter + neteq + send_loop verdes (debug RTF 1.26 < 2.5 gate, release 0.212 medido antes).
- Probe: frase completa, saltos 1, half 2 697/1470, eco 95.2 dB steady, ruido −80 dBFS, E: input 0.0409 → output 0.0687.
- Riesgo residual: eco audible los primeros ~1.3-2.5 s de cada llamada (tradeoff transparente, igual que Chrome/Meet) y tras cambios reales de camino de eco; el probe B global baja a 3.2 dB por el loop artificial (últimos 10 s 95.2 dB).

**Linked context:** plan `voice-mic-quality-plan.md`; pregunta "APM de chromium / parche C++"; reporte "raspado al final de palabras + volumen inestable en el sin-eco"

---

---
## 17:20 — voice: cadena final a producción (RNNoise descartado; AEC3 + NS VeryHigh + GC2 + limiter sin denoiser externo)

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` — `NoiseSuppressor`: `new()` = cadena APM pura (AEC3 + NS VeryHigh + GainController2 + limiter), sin denoiser externo ni leveler; `new_neural()` (DeepFilterNet opt-in) y `new_light()` (RNNoise opt-in) quedan como tiers. `apm_config()` sin parámetro neural; GC2 `AdaptiveDigital { headroom 5, max_gain 50, initial_gain 0, 3 dB/s, noise cap −50 dBFS }`. `process()` reescrito: branch neural (VAD LSNR) / rnnoise (VAD propio) / passthrough APM (speech_detected = RMS > 0.01); eliminados el campo `leveler`, el bloque de rescate pre-denoiser (quedaba inert tras el parche transparente) y `agc_gain()`.
- `crates/lumen-voice/src/client.rs` — sin cambios (ya usaba `new()`).
- `crates/lumen-voice/tests/agc_probe.rs` — reescrito: valida la cadena nueva (audibilidad en estado estable + sin varianza de nivel añadida vs el input; el GC2 necesita ~10 s de rampa → input speech.wav×5).
- `crates/lumen-voice/tests/deepfilter_probe.rs` — tier alto con `new_neural()`.
- `crates/lumen-voice/tests/audio_quality_probe.rs` — sin `agc_gain`/boosted frames; asserts sobre frames de habla RMS.
- `.gitignore` — `samples/` (177 MB de A/B regenerables por harness/probes).

**Summary:**
El usuario rechazó RNNoise como tier ligero ("le quita brillo a la voz y recorta palabras" — medido p10 −51.8 dB en frames quietos por su VAD). La cadena ganadora del A/B — AEC3 (parche transparente) + WebRTC NS VeryHigh + GC2 (AGC webRTC, rampa 3 dB/s) + limitador APM + `limit_peaks` — se porta a `process()` y pasa a ser `new()`. El leveler custom queda fuera de la ruta (era la fuente del "subidón"); el GC2 es el AGC. `speech_detected` pasa de LSNR VAD a RMS > 0.01 en el tier default (el medidor del cliente).

**Verification:**
- Suite completa verde: 26 lib + agc + deepfilter (75 s, RTF gate ≤ 2.5) + neteq + send_loop + voice_loopback; audio_quality_probe manual: habla 644/1801 frames (RMS flag), pico 18460 ≤ −1 dBFS, eco 21 dB residual (inaudible), ruido −18.4 dB, voz audible −24.4 dBFS; WAVs regenerados en samples/.
- Nota: con `initial_gain_db 0` el GC2 normaliza en ~5-10 s desde arranque (mics bajos llegan al nivel en ese lapso — tradeoff elegido para arranque limpio sin eco amplificado; ajustar `initial_gain_db`/`max_gain_change_db_per_second` en `apm_config()`).
- Disco: poda de generaciones viejas de binarios de test (~4.4 GB liberados) + scratch eliminados.

**Linked context:** plan `voice-mic-quality-plan.md`; A/B harness `tests/voice_harness.rs` (ignored); escuchas del usuario sobre chain_gc2_*; "RNNoise le quita brillo a la voz y recorta algunas palabras"

---
## 19:05 — voice: GC2 a defaults de referencia (initial 15 dB, 6 dB/s) — el favorito del usuario

**Category:** fix (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` — `apm_config()`: GC2 `initial_gain_db 0.0→15.0`, `max_gain_change_db_per_second 3.0→6.0` (los defaults de Chrome/Meet/Discord — el usuario confirmó que `chain_gc2.wav`, generado con esos valores, es SU favorito, no la variante inicial-0/3 dB/s). Test `noise_suppressor_attenuates_background_noise`: el noise-cap del GC2 converge en ~11-13 s (medido: −34 → −45 dBFS tras 13 s; antes de converger el ruido queda ~−37) → el test ahora mide el estado estable con 13 s de warmup y assert ≥ 8 dB.
- `crates/lumen-voice/tests/agc_probe.rs` — docs actualizadas (initial 15, 6 dB/s).

**Summary:**
El usuario escuchó los finalistas y su favorito es la cadena con los defaults de referencia del GC2 (initial 15, 6 dB/s) sobre el sample limpio — no la config inicial-0/3 que yo había portado ("arranque suave"). Cambio la producción a los valores de referencia. Medido: el arranque es +15 dB durante ~2-3 s hasta que el AGC asienta (el tradeoff de todas las apps WebRTC); el noise-cap tarda ~11-13 s en converger sobre ruido estacionario puro. Previews regenerados: fácil −21.6 dBFS/pico −1 (limitador), duro −26.6/pico −3.2 (la voz quieta llega al nivel más rápido con initial 15).

**Verification:**
- Suite completa verde: 26 lib + agc + deepfilter + neteq + send_loop + voice_loopback.
- Previews regenerados con la producción: `prod_chain_preview.wav` (≈ chain_gc2.wav — misma config) y `prod_chain_preview_hard.wav`.
- Decisión pendiente del usuario: escuchar el preview regenerado para confirmar que la producción == chain_gc2.wav; la opción DF atten20 (oscilación eliminada, piso −61.2) queda como tier opt-in si quiere probarla.

**Linked context:** escucha "Mi favorito no es prod_chain_preview_hard, es chain_gc2.wav"; A/B harness (GC2 defaults 15/6)

---
## 20:40 — voice: RTF DPDFNet — ORT 2.1x vs tract; INT8/fp16 sin ganancia en Haswell

**Category:** perf (audio quality)
**Affected files:** ninguno en producción — benchmarks de evaluación (python onnxruntime 1.28 + los probes tract existentes).

**Summary:**
El usuario pidió bajar el consumo de DPDFNet (compara con Krisp ~5% CPU). Medido en el i5-4590 (release, input duro de 36 s):
- DPDFNet 48k HR via **tract**: RTF 1.070 (107% de un core).
- Via **ONNX Runtime fp32** (el runtime que usa Krisp y sherpa): **RTF 0.507** — 2.1x, sin tocar el modelo (threads no ayudan: los GRU son secuenciales).
- **INT8** (quantize_dynamic): 0.698 — PEOR: el Haswell no tiene VNNI, el int8 en AVX2 se desparrama.
- **fp16**: inviable en CPU sin AVX-512 FP16.
- Variante 16 kHz (dpdfnet2, ORT): RTF 0.336 — solo 1.5x más barata y con el límite de banda a 8 kHz que el usuario rechazó ("encajonado").

**Conclusion:** el piso real del modelo en esta CPU es ~0.5 RTF (51% de un core ≈ 13% de la máquina de 4 cores). Krisp logra 5% con un modelo más pequeño + runtime optimizado — ya tenemos el runtime; el gap es el tamaño del modelo. La integración producción usaría el crate `ort` (runtime onnxruntime ~50-100 MB) en vez de tract para el tier DPDFNet.

**Verification:** benchmarks python con onnxruntime 1.28.0 (CPUExecutionProvider, ORT_ENABLE_ALL); el DSP idéntico al probe tract (ventana Vorbis exacta + WOLA knf); el modelo es el mismo archivo.

**Linked context:** "Krisp usa 5% de CPU total"; pregunta "hay alguna forma de que eso baje el consumo?"

---
## 21:30 — voice: investigación del presupuesto 7% CPU — GTCRN cumple con margen; LavaSR descartado

**Category:** perf (audio quality)
**Affected files:** ninguno en producción; benchmarks python (onnxruntime 1.28) + `samples/harness_gtcrn_hard36.wav` (escucha).

**Summary:**
El usuario fijó el objetivo: denoiser ≤ 7% de CPU total (RTF ≤ 0.28 en el i5-4590 de 4 cores). Investigación completa:
- **DPDFNet 48k**: piso medido 0.507 con ORT fp32 (2.1x vs tract); int8 PEOR (Haswell sin VNNI), fp16 inviable, threads sin efecto (GRU secuenciales), modo paralelo de ORT sin ganancia. No baja del presupuesto.
- **LavaSR** (Interspeech 2026, "50x realtime CPU", Apache-2.0): verificado real — pero procesa en **lotes de 1 segundo** (`wav_to_1s_batches`) + requiere torch: latencia inaceptable para llamada. Descartado.
- **GTCRN simple** (sherpa release, 0.5 MB, 16 kHz, STFT-in [1,257,1,2] + 3 caches, ventana hann²): **RTF 0.087 = 2.2% de la máquina** — 3.2x bajo el presupuesto. Calidad medida en el input duro: piso **−81.9 dBFS** (mejor que DPDFNet −70), voz −26.1 (3 dB más baja), pumping 0.752. Tradeoff: banda 16 kHz (el "encajonado" del roundtrip).
- **DF3 (tract, atten20)**: RTF 0.233 = 5.8% — ya cumple el presupuesto, 48 kHz full-band; el usuario nunca escuchó la variante atten20 (oscilación eliminada, pumping 0.794).

**Conclusión:** DPDFNet no cabe en 7% en esta CPU (modelo grande). Los candidatos dentro del presupuesto: GTCRN (2.2%, 16k, mejor piso) y DF3-atten20 (5.8%, 48k full-band). Camino futuro si se elige GTCRN: híbrido de máscara (máscara GTCRN aplicada al espectro 48k) → banda completa a ~2.5-3% (aún bajo presupuesto).

**Verification:** benchmarks onnxruntime 1.28 CPUExecutionProvider en el i5-4590, input duro de 36 s; el DSP GTCRN replicado del impl de sherpa (ventana hann² según metadata, WOLA knf, center-trim).

**Linked context:** "Quiero que llegue a mínimo 7% de CPU total"; "Krisp usa 5%"
## 23:30 — voice: conclusión investigación de modelos — FastEnhancer es el único realtime full-band

**Category:** perf (audio quality)
**Affected files:** `crates/lumen-voice/src/audio.rs`, `crates/lumen-voice/build.rs`, `apps/lumen-slint/` (select de modelo + auto-selección), `docs/dev-diary/2026-08-09.md`

**Summary:**
Cierre de la búsqueda de un denoiser tipo-Krisp (48 kHz full-band, tiempo real en el i5-4590, salida correcta). Medido y descartado: **DPDFNet 48k HR** (roto en onnxruntime 1.28 y TFLite — sobre-suprime a silencio; lento en tract RTF 1.07), DeepFilterNet (calidad rechazada), RNNoise (rechazado), GTCRN y los 16k (banda 8 kHz), LavaSR (lotes 1 s), aTENNuate/SUDO-RM (sin ONNX), NVIDIA RE-USE (licencia no-comercial). Búsqueda exhaustiva de HF (API + MCP oficial + buscador web): 88 repos "denoiser", ninguno cumple los 3 filtros salvo **FastEnhancer** (M/T/S/B a 48k). FastEnhancer-M (runtime C int8, MIT): RTF 0.252 = 6.3% en esta máquina (~2-3% en hardware moderno), correcto.

**Decisión integrada:** el tier neuronal por defecto es **FastEnhancer-M** (C runtime vendored, `with_model`), fallback **NS-only (WebRTC)** sin GTCRN; select en la UI con auto-selección por AVX2+FMA3+F16C y advertencia no-silenciosa cuando el modelo no corre. DPDFNet queda descartado con evidencia de 3 runtimes; se retoma solo si aparece un runtime que ejecute el modelo 2026.

**Verification:** `fe_probe` (ruido −37.9 dB, tono 76% preservado), suite lumen-voice verde, `cargo build --workspace`. Benchmarks python onnxruntime 1.28 / TFLite LiteRT / tract en el i5-4590.

**Linked context:** objetivo 7% CPU; "busca otro modelo que funcione realtime"; escucha A/B harness.

## 23:55 — fix(voice): mic capture "unsupported input format u8" — prefer i16/f32

**Category:** fix
**Affected files:** `crates/lumen-voice/src/audio.rs` (cpal_capture config selection)

**Summary:**
La app fallaba al entrar a una sala de voz con "mic capture" (context de `start_capture`). Causa: la selección de config de cpal elegía el primer config 48 kHz mono sin filtrar formato; pipewire-alsa anuncia u8 para el device por defecto (tras el restart del stack), y el build del stream solo soporta i16/f32 → "unsupported input format u8". Fix: la selección ahora salta formatos que no sean i16/f32. Verificado con test de reproducción (`start_capture` → OK).

**Verification:** `cargo test -p lumen-voice` (26 lib + probes, verde); workspace compila. Reproducción del fallo antes del fix y OK después.

**Linked context:** "al intentar entrar me sale mic capture y no me deja entrar"

## 00:10 — infra: Windows CI — webrtc fresh-build bug (pthread.h), RUSTFLAGS revertido

**Category:** infra
**Affected files:** `.github/workflows/ci.yml`, `.github/workflows/release.yml`, `crates/lumen-voice/build.rs`

**Summary:**
El job Windows del CI falla en **webrtc-audio-processing-sys** (`rtc_base/platform_thread_types.h(26): fatal error C1083: pthread.h`) en build FRESCO — el cache de GH venía tapándolo (el run previo tuvo 0 líneas de compilación de abseil). Mi `RUSTFLAGS=-C linker=lld-link` (para que fe enlace el .a GNU) invalidó los fingerprints → rebuild total → se destapó. `Cargo.toml`/`Cargo.lock` sin cambios: bug pre-existente del vendored, no de fe. **Decisión (opción C)**: revertir RUSTFLAGS → el cache vuelve a funcionar → Windows CI verde con tier **NS-only** (fe queda Linux/macOS). `build.rs` ahora compila fe en MSVC solo con lld-link; sin él, fallback NS-only.

**Follow-up pendiente (B):** arreglar el TU de webrtc que pierde `-DWEBRTC_WIN` en build fresco (meson del vendored, se itera por CI) para habilitar FastEnhancer en Windows.

**Linked context:** "compila en GH actions la versión de windows con FastEnhancer y NS para probar en pipeline de prod"
