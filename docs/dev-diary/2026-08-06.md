# Dev Diary — 2026-08-06

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 00:49 — Fix ghost voice peers: leave() now closes the signaling WS

**Category:** fix
**Affected files:**
- `crates/lumen-voice/src/signaling.rs`
- `crates/lumen-voice/src/client.rs`
- `crates/lumen-voice/Cargo.toml`
- `crates/lumen-voice/examples/leave_repro.rs`
- `apps/backend/src/do/ChannelDO.ts` (dedup, pre-existing uncommitted — deployed via wrangler)

**Summary:**
Two client bugs left ghost peers ("disconnected" tiles) after leaving/re-joining a voice channel: (1) `leave()` never closed the WebSocket — the heartbeat task holds a clone of the outbound sender, so the writer never saw the channel close; the DO therefore never broadcast `peer-left`. Fixed with a `SignalOut::Close` variant: the writer aborts the reader task and tears down the socket (dropping both split-stream halves is what closes the TCP). (2) `ServerMessage::PeerLeft` lacked `rename_all = "camelCase"`, so the DO's `{"peerId": ...}` failed to parse and the event was silently dropped. Also: `VoiceClient::join` now replaces a session whose run_loop ended on its own (socket death), fixing auto-reconnect which previously retried forever with "already in a voice channel". Verified live against production (leave_repro PASS, peer-left delivered) and backend smoke suite; deployed `lumen-backend` (version 89e8a4bd) with the DO dedup.

**Linked context:** voice signaling, production ghost-peer report

---

## 05:20 — Bound voice latency: drain-to-latest send loop + 2-frame jitter buffer

**Category:** fix
**Affected files:**
- `crates/lumen-voice/src/client.rs`
- `crates/lumen-voice/tests/send_loop_latency.rs` (nuevo)

**Summary:**
Tres cambios para acotar la latencia de voz: (1) el send loop ahora drena el canal del mic cada tick y conserva solo el frame más reciente (antes `try_recv` tomaba uno por tick; tras un stall del runtime se enviaban ráfagas de audio viejo — el remoto escuchaba lo dicho segundos antes). (2) El jitter buffer baja de 4 a 2 frames (80→40 ms de latencia base). (3) `MissedTickBehavior::Skip` en los tres tickers (send/levels/playout) para evitar catch-up en ráfaga. Tests nuevos miden la latencia del pipeline (35-61 ms, 20 frames) y prueban que el drain-to-latest deja 0 frames stale tras un burst de 50 (vs 49 el patrón viejo). 22 tests pasan.

**Linked context:** reporte de delay de voz VE↔AR

---

## 05:30 — Windows: suprimir ventana CMD en el cliente Slint

**Category:** fix
**Affected files:**
- `apps/lumen-slint/src/main.rs`

**Summary:**
`#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]`: los builds release de `lumen-desktop` se enlazan con el subsistema GUI, sin ventana de consola. Los builds debug conservan la consola (panics/backtraces visibles en `cargo run`).

**Linked context:** reporte de usuario Windows

---

## 05:35 — Tauri descontinuado

**Category:** docs
**Affected files:**
- `README.md`
- `docs/deploy.md`
- `docs/decisions/0002-slint-client.md`
- `.gitlab-ci.yml`

**Summary:**
`apps/desktop` (Tauri 2 + Svelte) marcado DEPRECATED/descontinuado; el cliente actual es `apps/lumen-slint` (bin `lumen`, release vía `.github/workflows/release.yml`). El job `windows-msi` de GitLab se deshabilita (`when: never`) para no gastar minutos de runner pagados en el MSI abandonado. El código Tauri se conserva como referencia hasta el corte (Fase 6).

**Linked context:** ADR 0002, Fase 6

---

## 05:50 — Adaptar NetEQ (jitter buffer adaptivo) al receive de voz

**Category:** fix
**Affected files:**
- `crates/lumen-voice/Cargo.toml`
- `crates/lumen-voice/src/audio.rs`
- `crates/lumen-voice/src/client.rs`
- `crates/lumen-voice/tests/neteq_spike.rs` (nuevo)

**Summary:**
Se reemplaza el `JitterBuffer` hand-rolled por el crate `neteq` (jitter buffer adaptivo estilo NetEQ de WebRTC: reordena, oculta pérdidas con PLC, time-stretch, delay adaptativo) en el path de recepción. Un `NetEq` por peer con nuestro `OpusDecoder` registrado vía el trait `codec::AudioDecoder` (puente `NetEqOpusDecoder`, i16↔f32). `on_track` hace `insert_packet`; el mixer hace `get_audio()` (2×10 ms por tick de 20 ms) y mezcla. Config: 48 kHz mono, `max_delay_ms=200` / `min_delay_ms=20` (sin clamp, un burst de jitter revivía delay de segundos). neteq con `default-features=false` (sin cpal/axum/tokio/ropus). Pendiente: playout device-callback-driven (causa raíz del corte) — neteq provee jitter, pero `get_audio()` aún lo llama el ticker del mixer. `JitterBuffer` viejo se conserva en audio.rs con sus tests como utilidad.

**Linked context:** reporte de delay y corte de voz VE↔AR; spike neteq validado previamente

---

## 06:30 — Send-path DSP: AEC3 + NS VeryHigh + RNNoise (echo y ruido de fondo)

**Category:** fix
**Affected files:**
- `crates/lumen-voice/Cargo.toml`
- `crates/lumen-voice/src/audio.rs`
- `crates/lumen-voice/src/client.rs`

**Summary:**
Dos problemas reportados con la novia (Venezuela↔Argentina): (1) **echo** porque usa parlantes — se habilita **AEC3** (`EchoCanceller::Full`, auto-delay) en el APM y se alimenta la referencia del playback: `AudioOutput` escribe una copia de lo reproducido en un `render_tap` (Arc<Mutex<Vec<i16>>>), y el send task la drena en `process_render_frame` antes de cada frame del mic (cap 500 ms). (2) **ruido de fondo** audible — config del APM: **NS VeryHigh + AGC OFF** (medido: 9.4× de atenuación de ruido estacionario vs 3.4× del config viejo; el AGC fijo de +9 dB/3 dBFS amplificaba el ruido quieto antes del NS) + **RNNoise** (`nnnoiseless`, pure-Rust, sin deps C) como 2ª etapa para ruido no estacionario (teclado/TV). Probes: AEC3 solo pasa señal 0.88×; señales sintéticas continuas se leen como ruido estacionario para NS/RNNoise (sin aserción de amplitud en tests de voz sintética). 24 tests pasan. Pendiente: configs de calidad en UI (knobs NS/AEC/AGC), playout device-callback.

**Linked context:** reporte de la novia (ruido de fondo + echo por parlantes); dirección: calidad de audio > Discord

---

## 07:10 — VAD-gated adaptive gain + voice-activation mode + real usernames

**Category:** fix
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (SpeechLeveler)
- `crates/lumen-voice/src/client.rs` (TransmitMode, set_transmit_mode, VAD gate)
- `crates/lumen-voice/src/event.rs`, `signaling.rs`, `lib.rs`
- `crates/lumen-voice/examples/{drop_peer,leave_repro}.rs`
- `apps/lumen-slint/src/{voice,controller}.rs`
- `packages/protocol/src/index.ts`
- `apps/backend/src/do/ChannelDO.ts`, `apps/backend/test/smoke-ws.mjs`

**Summary:**
Tres mejoras: (1) **SpeechLeveler** — gate + compresor con sidechain del VAD de RNNoise: sube la voz quieta a un nivel audible (RMS objetivo 0.12) SIN amplificar el ruido de fondo (medido: AGC2 adaptivo de WebRTC sí amplificaba el ruido +15 dB → rechazado; el gate solo abre ganancia cuando hay voz, con hangover 100 ms, y decae en silencio). (2) **VAD gate / modo de transmisión**: `TransmitMode::{Always, VoiceActivated}` + `VoiceClient::set_transmit_mode`; por defecto VoiceActivated (no transmite silencio). (3) **Nombres reales de participantes**: el protocolo de signaling ahora lleva `username` en `join`/`PeerInfo`/`peer-joined` (el cliente lo manda; el DO lo guarda y reenvía); la UI muestra el username en vez del `user_id.take(8)`. Compat: `#[serde(default)]`. 26 tests pasan; typecheck protocol+backend OK; CI 3 OS + Release OK.

**Linked context:** reportes del usuario (ruido, mic siempre abierto, nombres en UI); dirección: calidad de audio > Discord

---

## 07:20 — Spike GTCRN/sherpa-onnx (candidato a supresor de ruido profesional)

**Category:** feature (investigación)
**Affected files:** ninguno (spike en /tmp/gtcrn-spike)

**Summary:**
Evaluación de GTCRN (denoiser neuronal) vía `sherpa-onnx` 1.13.4 como reemplazo de RNNoise. Resultados en i5-4590 (release): **~10× tiempo-real** (RTF 0.086-0.135), **32-38 dB de reducción de ruido** (RNNoise daba ~1× en ruido estacionario), modelo `gtcrn_simple.onnx` de **535 KB**, API online/streaming (frames de 256 muestras @16k, auto-resample), **prebuilts para los 3 OS** (Linux/macOS/Windows, sin cmake/onnxruntime build — auto-download 22 MB). Costo: dependencia nativa onnxruntime (~decenas MB), ~9× margen de CPU. **Recomendación: integrar en lumen-voice** (reemplaza/suple RNNoise) — validado, sin blockers. Pendiente: decisión del usuario + integración.

**Linked context:** dirección calidad de audio > Discord; spike delegado a subagente

---
