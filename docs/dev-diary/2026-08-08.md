# Dev Diary — 2026-08-08

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 01:30 — voice: estabilizar AGC — VAD por LSNR + post-filter + rate limiter + probe

**Category:** feature (audio quality)
**Affected files:**
- `crates/lumen-voice/src/audio.rs` (`DeepFilterDenoiser::process` → `(Vec<i16>, f32)`, VAD por LSNR en el tier neural, post-filter 0.02, rate limiter de ganancia ±0.3/frame, `HANGOVER_FRAMES` 12→25, `max_gain` 6→8, accessors `gain/speech_rms/vad_smooth/agc_gain/last_lsnr`, removido `SPEECH_ENERGY_REF`)
- `crates/lumen-voice/tests/agc_probe.rs` (reescrito: una sola cadena, columnas reales raw_rms/lsnr/agc_rms/gain/speak, asserts de estabilidad y volumen)
- `crates/lumen-voice/tests/deepfilter_probe.rs` (migración a la nueva firma de `process`)
- `crates/lumen-voice/tests/neteq_spike.rs` (warning preexistente `unused variable`)
- `crates/lumen-voice/testdata/speech.wav` (reemplazado: el fixture espeak era 98% silencio — 2 ráfagas ≤3 frames — insuficiente para ejercitar el AGC; nuevo fixture determinista 3.95 s: stack armónico con formantes y ritmo de palabras, ~108 frames de voz en runs de 5-8 frames)

**Summary:**
Dos problemas del send path: volumen bajo en mics silenciosos y "montaña rusa" de ganancia durante el habla. El AGC se auto-conducía con un VAD ruidoso (`rms_level(denoised)`).

- **VAD por LSNR:** `DeepFilterDenoiser::process` devuelve ahora `(pcm, lsnr)` — el LSNR (dB) interno de DeepFilterNet (se descartaba con `let _`). El tier neural deriva el VAD del LSNR: `vad = ((lsnr - (-10.0)) / 40.0).clamp(0,1)` (rango modelo: < -10 ruido, > 30 voz limpia). No confunde un hablante silencioso (RMS bajo, LSNR alto) con silencio. En error/padding se usa el centinela del modelo (−15 dB). RNNoise sigue con su VAD propio. `SPEECH_ENERGY_REF` eliminado (su único uso era el VAD neural; ningún test lo usaba).
- **Post-filter:** `RuntimeParams::default().with_post_filter(0.02)` — sobre-atenuación espectral por bin, aprieta el residual en bins adyacentes al habla; costo medido nulo (A/B debug: 0.432 vs 0.536 RTF — ruido de medición, sin delta).
- **Rate limiter de ganancia:** tras el IIR asimétrico (τ 200/300 ms), el cambio por frame se limita a ±0.3 (~15 dB/s): onset rápido sin que una sílaba fuerte baje la ganancia 30% en un frame. `HANGOVER_FRAMES` 12→25 (500 ms, cubre pausas entre frases) y `max_gain` 6→8 (+18 dB, seguro porque el VAD por LSNR no abre el gate con ruido; un mic a RMS 0.015 llega al target 0.12).
- **Probe agc:** reescrito (estaba roto: creaba un `NoiseSuppressor` por frame e imprimía "—"). Una sola cadena secuencial; tabla cada 0.5 s y resumen final. Asserts nuevos: estabilidad de ganancia en ventanas de voz contiguas (≥5 frames, excluida la rampa de ataque) `max/min < 2.0`, y volumen `avg output RMS (speech) ≥ 0.04`.

**Verification:**
- `cargo build -p lumen-voice`: 0 warnings de lumen-voice (solo dep webrtc build-script).
- `cargo test -p lumen-voice --lib`: 21/21 ok (incl. `speech_leveler_boosts_quiet_speech_not_silence` y `speech_leveler_gates_silence_with_hangover` con las constantes nuevas; `deepfilter_path_speech_detection_by_lsnr` renombrado).
- `cargo test -p lumen-voice --test agc_probe -- --nocapture`: LSNR 28-35 dB en voz (−15 en silencio), gain ~2×, avg output RMS (speech) **0.0799** ≥ 0.04 ✓, worst window ratio **1.30** < 2.0 ✓.
- `cargo test -p lumen-voice --test deepfilter_probe`: ok (gates RTF debug < 0.5, latencia ≤ 80 ms, on_db > light_db ≥ 40 dB). Post-filter A/B: sin costo medible.
- `cargo build -p lumen-desktop`: ok (smoke del app; `lumen-slint` fue renombrado a `lumen-desktop`).

**Linked context:** plan AGC stabilization (aprobado); dirección calidad de audio > Discord; reporte "volumen bajo / montaña rusa" en mics silenciosos

---
