# Dev Diary — 2026-08-05

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 11:05 — Fase 0-1: desacople del voice core (migración Slint)

**Category:** refactor
**Affected files:**
- `docs/decisions/0002-slint-client.md` (nuevo ADR)
- `Cargo.toml` (nuevo workspace raíz)
- `crates/lumen-voice/Cargo.toml`, `src/{lib,event}.rs` (nuevos)
- `crates/lumen-voice/src/{client,audio,signaling,rtp}.rs` (movidos de `apps/desktop/src-tauri/src/voice/`)
- `crates/lumen-voice/tests/voice_loopback.rs` (movido de `apps/desktop/src-tauri/tests/`)
- `apps/desktop/src-tauri/src/lib.rs` (TauriAdapter)
- `apps/desktop/src-tauri/Cargo.toml`, `Cargo.lock` (movido a raíz)

**Summary:**
Inicio de la migración a Slint (ADR 0002). Se extrae el cliente de voz nativo a
`crates/lumen-voice`, un crate framework-agnóstico (sin Tauri/UI): `AppHandle`
se reemplaza por `mpsc::UnboundedSender<VoiceEvent>` (enum tipado que refleja
1:1 los 7 canales `voice://` legacy). La app Tauri construye un `TauriAdapter`
que re-emite payloads JSON idénticos, así el frontend Svelte queda intacto
durante la transición. Se crea workspace Cargo raíz.

**Verification:** `cargo build --workspace` ok (lumen + lumen-voice); 14 tests
unitarios ok; `voice_loopback` (DTLS/SRTP real, `--ignored`) ok.

**Linked context:** Fase 0-1 del plan de migración Slint (rama `slint-client`)

---
