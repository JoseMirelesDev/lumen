# Dev Diary — 2026-08-10

> Auto-maintained by dev-diary skill. One entry per approved change.

---

## 19:50 — ui: re-theme Vox medieval-war (Slint) — paleta crimson+gold, fonts embebidos, sonidos, estados de voz, compact grid

**Category:** feat (ui/ux)
**Affected files:**
- `apps/lumen-slint/ui/theme.slint` (reescrito: paleta medieval-war completa — crimson `#9E312A` / ember `#D95B40` / gold `#E0B36A` en vez del naranja vikingo; text-mute subido a `#8F91AF` por AA; tokens de motion `pulse`/`breath`/`step-*` + `motion-reduced`; fonts vía `import "../fonts/*.ttf"`)
- `apps/lumen-slint/ui/icons.slint` (nuevo: codepoints de Phosphor — MIT, `@phosphor-icons/web` regular — para todos los iconos)
- `apps/lumen-slint/ui/base.slint` (primitivas: VoxButton con estados 8, IconButton+tooltip, VoxLineEdit con placeholder manual, Bust con pulso crimson↔gold + dim overlay, VoiceMeter cuantizado, SpeechBubble, PixelFrame con ticks HUD, StateChip, StatusDot con formas codificadas)
- `apps/lumen-slint/ui/{server-rail,channel-list,friends-view,chat-view,voice-view,login,modal,app}.slint` (re-diseño completo; vistas nombradas `channels := ChannelList …` para el MCP; channel-list con modelo único + `height: 0` en filas del otro kind)
- `apps/lumen-slint/src/{model,controller,voice,sound,main}.rs` (skins por hash de nombre; wiring de sonidos; player cpal `sound.rs`; `read_os_reduced_motion`)
- `apps/lumen-slint/sounds/*.wav` + `scripts/gen-ui-sounds.py` (kit de 12 sonidos sintetizados, 48 kHz mono i16, embebidos con include_bytes)
- `apps/lumen-slint/fonts/` (9 pesos estáticos Cinzel/Inter/JetBrains Mono instanciados con fontTools desde OFL variables + `Phosphor.ttf` + licencias)
- `apps/lumen-slint/assets/pixel/` + `scripts/gen-pixel-assets.py` (51 placeholders: bustos/personajes/fogata/asientos/runa recolorizados de los masters de `designs/`)
- `apps/lumen-slint/build.rs` (`EmbedResourcesKind::EmbedFiles` — fonts+imágenes dentro del binario)
- `docs/vox-html-to-slint.md` (diccionario HTML/CSS→Slint, incluye el bug de `visible:false` en layouts)
- `.omp/mcp.json` + `scripts/lumen-mcp.sh` (MCP server de Slint: `--features slint/mcp`, `SLINT_MCP_PORT`)

**Summary:**
Aplicación del design system Vox (`designs/`) al cliente Slint con re-anclaje de la energía: el ámbar/naranja vikingo pasa a **crimson + gold heráldico** ("hordas y legiones", no vikingos) — crimson solo en fills/bordes (2.6:1, nunca texto pequeño), ember como único rojo legible en texto (5.0:1), gold como peak/acento (9.7:1). Tipografía: Cinzel (display ≥18px, nunca body) + Inter + JetBrains Mono, embebidas. Iconografía: Phosphor como font (MIT, 1600+ iconos — escala para features futuras). Sonidos: kit minimalista (join/leave/mute/deafen/send/receive/user-join/user-left/error/call) reproducido por cpal, sin loops ni melodías. Estados de voz: busto con pose swap + borde crimson↔gold a pasos + espectro de micrófono cuantizado + speech bubble con glyph (hover → nombre); muted = brazos cruzados + dim + borde bronce; deafened = chip ear-slash; video tile con ticks HUD listo para el pipeline futuro. Compact grid auto >10 usuarios con grupos HABLANDO/EN ESCUCHA/MUTEADOS, scrollable. Bug clave resuelto: `visible: false` NO quita elementos del layout en Slint 1.17 → filas del otro tipo colapsadas a `height: 0` (verificado por árbol MCP: grupos empaquetados sin huecos).

**Verification:**
- `cargo build -p lumen-desktop --features slint/mcp` limpio (0 warnings del app).
- MCP server (`SLINT_MCP_PORT=8080`): `list_windows`/`get_element_tree`/`take_screenshot` responden; árbol del ChannelList muestra filas empaquetadas (y=57 header → y=81 fila → y=125 header → y=149 fila, huecos 0).
- `slint-viewer --screenshot` (1.17.1 instalado) renderiza VoiceView standalone con fogata animada + asientos; compact grid con 12 peers renderiza headers+filas+grupos.
- App viva con sesión real (backend workers.dev): shell 3 columnas OK.

**Linked context:** diseño de `designs/` (vox-spec.pdf, vox-mockups), veredicto del designer (paleta, tipografía, estados), pedido del usuario (no-vikingo, hordas y legiones, sonido minimalista, estados interactivos, compact grid con personalidad)
## 23:19 — ui: paleta ice/rose, header canales, settings panel, auto-join, flat compact, friends, modales

**Category:** feature
**Affected files:**
- `apps/lumen-slint/ui/theme.slint` (paleta: gold/amber → ice #86B9CE, ember → rose #D1635E; token rename gold→ice, ember→rose en toda la UI)
- `apps/lumen-slint/ui/base.slint` (iconos de VoxButton/IconButton con nudge óptico; nuevo HeaderIconBtn 26px; VoxLineEdit variante flat)
- `apps/lumen-slint/ui/channel-list.slint` (nombre a fila propia 18px + botones 26px en fila del tag; feedback copiado link→check con Timer; footer gear en vez de logout)
- `apps/lumen-slint/ui/voice-view.slint` (sin botón join en header; chip de estado semántico icono+texto; CTA "ÚNETE A LA FOGATA" centrado cuando desconectado; CompactGrid plano por orden de entrada con estado visual por fila; callbar con globo VoiceMeter+label; fila dev Suppression/AEC fuera)
- `apps/lumen-slint/ui/settings.slint` (nuevo: panel de ajustes 420x480 con secciones AUDIO/CUENTA/ACERCA, ScrollView escalable, logout dentro)
- `apps/lumen-slint/ui/app.slint` (mount SettingsView + settings-visible in-out; wiring open-settings)
- `apps/lumen-slint/ui/modal.slint` + `login.slint` (fix colapso/estiraje: height explícita en tarjeta + Math.min; causa raíz del modal/login roto)
- `apps/lumen-slint/ui/chat-view.slint` (fix mensajes de continuación invisibles: horizontal-stretch en columna; composer con input flat)
- `apps/lumen-slint/ui/friends-view.slint` (reorden: SOLICITUDES primero; headers con marcador cuadrado; separadores; espaciado)
- `apps/lumen-slint/src/controller.rs` (auto-join de voz al seleccionar canal voice; copy-invite con log de errores; reset settings-visible en logout)

**Summary:**
Iteración de review de UI: paleta re-anclada de dorado/naranja a ice+rose (contraste verificado AA); el header de canales deja el nombre a ancho completo; join pasa a un CTA centrado y el canal de voz auto-conecta al entrar; la lista compacta es plana por orden de entrada con estado visual por fila; settings panel nuevo (migra Suppression/AEC/logout del header de voz y footer); modales y login arreglados (causa raíz: tarjeta sin height explícita colapsaba a 0 o rellenaba el padre); mensajes de continuación visibles; iconos de botones con nudge óptico.

**Linked context:** review de UI del cliente Slint (batch del usuario: header, join, compact, settings, friends, copy, status, colores)

---
## 23:59 — ui: feedback batch 2 — compact align, meter floor, modal rework, clipboard, chat msg fix, SVG oficiales

**Category:** fix
**Affected files:**
- `apps/lumen-slint/ui/chat-view.slint` (fix real del mensaje de continuación: el author-row con hijos `if`-ocultos colapsaba la columna a 0 — envolver el row en `if m.first-in-run`; SendButton 36px)
- `apps/lumen-slint/ui/base.slint` (VoiceMeter cross-axis-alignment:end → espectro anclado abajo; VoxButton padding según contenido: icon-only 7px / compact 8px)
- `apps/lumen-slint/ui/voice-view.slint` (CompactRow alignment:start + busto cuadrado 48x48; Seat a ratio 16:22 (72x99) sin deformar; CampfireScene `connected` → sin seat/roster al desconectar; CTA con icono centrado)
- `apps/lumen-slint/ui/modal.slint` (backdrop 65% + TouchArea cierra al tocar fuera; botones uniformes 72x28; título con altura fija; tarjeta 340px)
- `apps/lumen-slint/ui/settings.slint` (backdrop 65%)
- `apps/lumen-slint/ui/friends-view.slint` (padding-top 16px + spacing 4px entre secciones)
- `apps/lumen-slint/src/controller.rs` (clipboard arboard persistente en `static CLIPBOARD: LazyLock<Mutex<Option<Clipboard>>>` — el handle dropeado = selección X11 vacía; copy ahora funciona)
- `scripts/gen-svg-icons.py` (descarga SVGs oficiales de phosphor-icons/core `assets/regular` con fallback TTF; flip Y + bbox por glyph)

**Summary:**
Segunda iteración de feedback: espectro del meter anclado al suelo, compact list alineada a la izquierda con avatares cuadrados y sin deformar (ratio del pixel-art), escena de voz limpia al desconectar (el seat/nameplate ya no asoma bajo el CTA), modales con cierre al tocar fuera + botones uniformes + título compacto, composer alineado, secciones de Friends con aire, y el botón de copiar arreglado (el handle del portapapeles vivía menos que la selección X11). Los iconos de la app pasan a ser los SVG oficiales de Phosphor.

**Linked context:** feedback de iteración del usuario (batch 2)

---
## 00:40 — refactor: arquitectura de primitivas + overlays + higiene (codebase-memory indexado)

**Category:** refactor
**Affected files:**
- `apps/lumen-slint/ui/primitives/{layout,chips,buttons,inputs,identity,meters,icon}.slint` (nuevos: split de base.slint en 7 módulos por dominio)
- `apps/lumen-slint/ui/base.slint` (eliminado)
- `apps/lumen-slint/ui/voice-parts.slint` (nuevo: CallBtn + ViewToggle extraídos)
- `apps/lumen-slint/ui/voice-view.slint` (969 → ~838 líneas; VideoTile/PixelFrame/ramas video eliminadas)
- `apps/lumen-slint/ui/shell.slint`, `ui/preview-voice.slint` (eliminados — muertos)
- `apps/lumen-slint/ui/app.slint` (overlay manager: 4 booleans → `overlay: "none|create-server|join-server|create-channel|settings"`)
- `apps/lumen-slint/ui/theme.slint` (tokens nuevos: dim, backdrop, panel-translucent, talking-edge, muted-edge, error-tint; embed de Phosphor.ttf fuera)
- `apps/lumen-slint/src/controller.rs` (set_overlay en logout)
- `apps/lumen-slint/ui/base.slint` → IconButton/SpeechBubble eliminados (muertos)

**Summary:**
Aplicación de la auditoría de arquitectura: split de base.slint en módulos de primitivas por dominio (layout/chips/buttons/inputs/identity/meters/icon) — las vistas importan solo lo que usan; extracción de voice-parts; overlay manager único (un slot Z, un mount, un close — reemplaza los 4 booleanos que permitían overlays simultáneos sin orden); limpieza de muertos (shell, preview, IconButton, SpeechBubble, PixelFrame, VideoTile, canal video); tokens para los 12 hex sueltos; Phosphor.ttf ya no se embebe (iconos SVG). Repo indexado en codebase-memory (2915 nodos). Verificado con MCP en vivo: shell, modales y settings vía overlay.

**Linked context:** auditoría de arquitectura de la UI (rondas del skill software-architecture: higiene → split → overlay)

---
