{
  "name": "@lumen/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared types for the Lumen signaling protocol and REST API (single source of truth).",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo ok",
    "test": "echo ok"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}
